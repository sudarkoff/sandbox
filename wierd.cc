#include <iostream>

struct X {
    static bool f( int* p ) 
      { return p && 0[p] and not p[1:>>p[2]; }
};

int main(int argc, char* argv[])
{
	int a = 10;
	std::cout << ((X::f(&a))? "yes": "no") << std::endl;
	return 0;
}
